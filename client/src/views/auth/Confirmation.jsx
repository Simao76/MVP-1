/* import React from 'react';
import { Link } from 'react-router-dom';

const Confirmation = props => {
  console.log(props.user)
  return (
    <div>
      {props.user && (
        <div>
        <p>Welcome to MPV {props.user.name}, your home for sports stats, news and more</p>  
        <p>Check and edit your <Link to={`/profile/${props.user.name}`}>profile</Link>, your home for sports stats, news and more</p> 
        </div>
      )} 
    </div>
    
  )
}

export default Confirmation; */
